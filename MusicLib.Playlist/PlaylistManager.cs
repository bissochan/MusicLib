using MusicLib.FileM;

namespace MusicLib.Playlist;

public class PlaylistManager(string rootDirectory)
{
    private string _rootDirectory = rootDirectory;

    public void CreatePlaylist(List<SongData> songData)
    {
        
    }
}